/**
 * Library version, surfaced by the CLI and the web API `/api/health`.
 * Kept in sync with package.json at release time.
 */
export const VERSION = '1.1.0';

/** Product name, used in headers and help text. */
export const PRODUCT_NAME = 'agent-control-tower';
